{
  "name": "seobetter-cloud-api",
  "version": "1.0.0",
  "private": true,
  "description": "SEOBetter Cloud API proxy — AI content generation for WordPress plugin users without their own API keys",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  }
}
